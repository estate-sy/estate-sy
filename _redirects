# ══════════════════════════════════════════════════════════════════
# Netlify _redirects
# يحوّل الرابط النظيف /p/PROPERTY_ID إلى الدالة الخادمية التي تُرجع
# وسوم Open Graph الصحيحة، ثم تُحوّل الزائر البشري تلقائياً لصفحة التفاصيل الحقيقية.
# ضع هذا الملف في جذر مجلد النشر (نفس مكان index.html)
# ══════════════════════════════════════════════════════════════════

/p/:id  /.netlify/functions/property-og?id=:id  200
